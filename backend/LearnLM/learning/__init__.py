"""
learning — v2 home for pure learner-model math (frozen architecture §9).

Modules here contain domain logic only: no HTTP, no ORM queries — DB
state arrives as plain values or passed objects. The legacy groups
module shrinks into this package by extraction (strangler pattern).
"""
