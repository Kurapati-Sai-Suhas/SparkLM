# --- FRONTEND (React/Node) ---
node_modules/
dist/
dist-ssr/
build/
.env
.env.local
*.local
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# --- BACKEND (Django/Python) ---
*.pyc
__pycache__/
*.sqlite3
db.sqlite3
media/

# --- VIRTUAL ENVIRONMENTS ---
env/
env1/
venv/
.venv/

# --- ML MODEL ARTIFACTS ---
models_data/
*.pkl
*.pth
*.onnx

# --- SYSTEM & EDITOR FILES ---
.DS_Store
.vscode/
!.vscode/extensions.json
.idea/
Thumbs.db
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
# Runtime artifacts from reseed_questions
reseed_failures.json
staticfiles/

# Role DDL carries a generated password
backend/LearnLM/sql/learnlm_*.sql

# ── Grading truth must never enter this repository (M2 P2.7h-33) ──────────
#
# This repository is PUBLIC. Hidden-test inputs, their expected outputs and
# reference solutions are the answer key for questions that are served to
# learners and graded. The database is the source of truth for all of it;
# nothing here needs a copy.
#
# Phase 19 committed the pilot suite plans, and the checkpoint before it
# carried suite expansions and a reference solution for two questions that
# are PUBLISHED and adaptive-eligible. Both were untracked in Phase 20.5.
#
# Statements, starter boilerplate and quality specs (which hold MUTANTS —
# wrong solutions — not answers) are deliberately NOT ignored: those are
# public by design.
artifacts/**/references-*/
artifacts/**/suites-*/
backend/LearnLM/remediation/*_suite_expansion.json
backend/LearnLM/remediation/*_approved_cases.json
backend/LearnLM/remediation/*_case*_input.json
backend/LearnLM/remediation/*_reference_*.py
pilot_*/reference_*.py

# The reference-authoring directory `reference_create --source-file` reads
# from (M2 P2.33). Ignored as a WHOLE DIRECTORY rather than by filename
# pattern: the rules above each name a shape someone had to remember, and a
# reference solution saved as `q7.py` instead of `q7_reference_python.py`
# would have been committed to a public repository. The directory's only
# purpose is to hold answer keys, so nothing in it may ever be tracked.
backend/LearnLM/references/

# ── KT research corpora and checkpoints (M2 P2.12) ────────────────────────
#
# ASSISTments' terms require a written agreement, prohibit redistribution and
# require publishing your source code. Acquiring the file is an operator
# decision; REDISTRIBUTING it through this public repository is not ours to
# make, and a subset is still the dataset. The manifest records the file's
# sha256, which is how a result stays checkable by someone who obtained their
# own copy — neither party has to send the data.
#
# Checkpoints are binary, regenerable from config + seed, and derived from
# that same licensed corpus. The metrics they produced ARE committed, under
# kt_research/results/ — those are the findings.
backend/LearnLM/kt_research/data/
backend/LearnLM/kt_research/checkpoints/

# Interview preparation — personal study material, deliberately not published.
# These live on disk and are regenerable; they are kept out of the public
# repository because they are notes for one person, and because the prep
# documents cite the specific commits carrying the historical credential
# exposure, which is a signpost this repository should not carry.
docs/SPARKLM_INTERVIEW_PREP.md
docs/SPARKLM_INTERVIEW_CHEATSHEET.md
docs/SPARKLM_MOCK_INTERVIEW.md
docs/SparkLM_Interview_Complete.docx
docs/SparkLM_Interview_Complete.pdf
docs/10_INTERVIEW_HANDBOOK_PART_*.md
